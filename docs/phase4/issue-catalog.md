# Phase 4 Issue Catalog

Mismatches discovered while running the parity scenarios (review §Issue
Catalog Additions). Severity rules: Critical = endura cannot safely
replace production behavior; High = replacement needs a risky
workaround; Medium = acceptable for a narrow pilot, not broad rollout;
Low = cleanup / documentation / non-blocking parity gap.

| ID | Severity | Area | Current Driver App Behavior | Endura Behavior | Production Risk | Required Fix | Scenario |
| -- | -------- | ---- | --------------------------- | --------------- | --------------- | ------------ | -------- |
| P4-003 | High — **FIXED** (phase-4-parity) | Engine concurrency / uniqueKey | RNQ start() dedupe is a read-then-enqueue on a mostly-synchronous Realm path; concurrent enqueues effectively serialize on the JS thread. | Two bugs found by scenario 7's racing-duplicate step: (1) the drivers' `transactionDepth` join treated a CONCURRENT operation as a nested one, so an engine op interleaving on the event loop silently joined a stranger's open transaction — the loser's unique violation rolled back the winner's rows and start() returned a ghost execution (`UniqueConstraintError ... active execution: unknown`); (2) start()'s DB-violation translation path ignored `onConflict:'ignore'` and always threw. | Any concurrent same-client operations (two starts, start during tick) could cross-wire commits/rollbacks — silent data loss well beyond dedupe. | Fixed: engine-level operation lock (`runExclusive`) serializes all storage-transactional segments + the task claim (user activity code stays outside the lock), and the violation-translation path now honors `onConflict:'ignore'` by returning the winner. Regression tests: UniqueKey.test.ts racing-duplicate cases. | 7 |
| P4-004 | Low | Callback placement | RNQ worker lifecycle callbacks (onSuccess/onFailed) run outside any DB transaction; workers freely enqueue other pipelines from them. | Activity `onSuccess`/`onFailed` callbacks execute INSIDE the engine's transactional segments (WorkflowEngine failure/advance transactions), which now also hold the P4-003 operation lock — a callback that calls an engine write API (e.g. `start()` to chain a pipeline) deadlocks. Chaining from ACTIVITY code is safe. | Consumers porting driver-app "on success, enqueue next pipeline" patterns into callbacks would hang the engine. Documented on runExclusive; low because activity-code chaining is the recommended pattern and works. | Move user-callback dispatch outside the transaction boundary (after commit), so callbacks are ordinary post-write hooks that may safely call engine APIs. | 7 |
| P4-005 | Medium — push API added (phase-4-parity) | Connectivity freshness | isJobRunnable reads NetInfo-backed state effectively live at every queue poll — a network drop is visible to the very next runnability check; jobs hold instead of attempting. | ExpoEnvironment's connectivity was a 1s-interval polled cache defaulting to TRUE (async initial refresh), so after a drop (or on cold start while offline) runWhen gates saw a stale "online" for up to ~1s and executed guaranteed-fail requests, burning real retry attempts — scenario 8 exhausted a 3-attempt budget and dead-lettered a job that should have been held. | Small-budget connectivity-gated workflows can dead-letter during the staleness window after every network drop; cold-start-offline burns attempts immediately. | Added `ExpoEnvironment.setNetworkState(connected)` — the push counterpart to the polled provider (NetInfo-listener pattern, matching setAppState). Consumers should wire `NetInfo.addEventListener(s => environment.setNetworkState(!!s.isConnected))`; keep the polled provider only as a fallback. Remaining: document as the recommended pattern; consider awaiting the initial provider read in create(). | 8 |
| P4-006 | Low — **FIXED** (phase-4-parity) | Observability / stale results | The driver app's stale-failure guards make ignored results visible: markEventAsFailed checks staleness and logs when a late failure is discarded, so "the stale result was ignored" is auditable in production logs. | The engine's timeout race already guaranteed a late settlement could not overwrite newer state, but the drop was SILENT — `executePromise.catch(() => {})` swallowed the eventual settlement with no log line. Scenario 11's observability assert ("the discard is visible in engine logs") failed with an empty log set while every correctness assert passed. | Hung/timed-out handlers that eventually settle are invisible in production logs — no way to distinguish "handler never finished" from "finished late and was correctly ignored" when debugging field issues. | Fixed: when the abort wins the race while the handler is still pending, continuations on the handler promise log `Discarding late success/failure from timed-out or aborted attempt` (info) with taskId/activityName/runId/attempt. Regression tests: WorkflowEngine.staleDiscard.test.ts. | 11 |
| P4-002 | Low | Recovery classification | Each pipeline definition carries `recoverable: false` where applicable (photoReaper, sendAppDump), and recovery.ts reads it off the definition — classification and definition live together (`photoReaper/index.ts:33`). | `Workflow` has no definition-level classification or options field (`types.ts:320-333`), so "never re-arm this workflow" must live in a separate app-layer set the recovery sweep consults (see `parity-app/scenarios/nonRecoverablePipeline.ts` NON_RECOVERABLE_WORKFLOWS). | The workaround works but re-creates the driver app's hand-maintained-list failure mode in miniature: adding a fire-and-forget workflow without updating the set silently makes it driver-recoverable. Low because the sweep itself is app-layer anyway and the set sits beside the definitions. | Add an optional `Workflow.recovery?: 'recoverable' \| 'non-recoverable'` (or a generic `Workflow.options`) stamped onto executions/dead letters so sweeps can read classification off the record instead of a parallel list. | 6 |
| P4-001 | Medium | Activity registration | RNQ worker names are globally namespaced too, but every pipeline avoids collisions by convention: worker names carry the pipeline prefix (`outcomeSubmit.stage1CreateDraft` vs `outcomeWorkflowDataSync.stage1CreateDraft`), and the extraction notes the distinct names are load-bearing (they are what stops the queue from auto-advancing mid-fill sync into a submit stage). | `WorkflowEngine.registerWorkflow` rebuilds one global `activities: Map<name, Activity>` across ALL workflows (`WorkflowEngine.ts:270-275`). Same-named activities in two different workflows silently last-win — tasks for workflow A execute workflow B's closure. Only same-`workflow.name` re-registration logs a warning; cross-workflow activity collisions produce no signal. Task execution (`WorkflowEngine.ts:660,1159,1329`) resolves by bare activity name even though the task row knows its workflow. | A team porting two sibling pipelines (exactly the outcomeSubmit / outcomeWorkflowDataSync pair) with natural stage names gets silent cross-wiring: behavior diverges only where the closures differ, with no error, warning, or DLQ entry. Found because scenario 3's per-chain run counters landed under the wrong chain while every business assertion still passed. | Either (a) scope activity resolution per workflow — the task already carries `workflowName`, so resolve through `workflow.activities` as `advanceWorkflow` already does at `WorkflowEngine.ts:236` — or (b) throw (not warn) on cross-workflow activity-name collision at `registerWorkflow` time. (b) is the smaller change and matches the driver app's de-facto contract of globally unique stage names. | 3 |
